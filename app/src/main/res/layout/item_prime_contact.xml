<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:padding="8dp"
    android:background="@drawable/bg_prime_badge">
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">
        <TextView android:id="@+id/primeItemName" android:textColor="#EEEEEE" android:textSize="14sp" android:textStyle="bold" />
        <TextView android:id="@+id/primeItemNumber" android:textColor="#888888" android:textSize="12sp" />
    </LinearLayout>
    <ImageButton android:id="@+id/primeItemDelete" android:layout_width="32dp" android:layout_height="32dp" android:src="@drawable/ic_close" android:background="@android:color/transparent" />
</LinearLayout>
